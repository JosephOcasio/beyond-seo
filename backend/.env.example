## Example environment variables for Reasoner + Prototype API
## Load with:
##   set -a; source backend/.env; set +a

# Optional: set explicitly if you run commands outside the repo root
PROJECT_ROOT=

# Auth/RBAC
RBAC_ENFORCE=true
JWT_SECRET=change_me
JWT_ALGORITHM=HS256
JWT_AUDIENCE=
JWT_ISSUER=

# Celery/Redis (adjust if you run a Redis instance)
REDIS_URL=redis://localhost:6379/0

# Prototype persistence (PostgreSQL preferred in production)
# Examples:
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/master_scribe
# DATABASE_URL=sqlite:////absolute/path/prototype.db
DATABASE_URL=
AUTO_INIT_DB=true

# OCR / Vertex settings for image/pdf ingestion
PROJECT_ID=vivid-lambda-483212-s2
DEEPSEEK_MODEL=deepseek-ai/deepseek-ocr-maas
VERTEX_LOCATIONS=global,us-central1
VERTEX_AUTH_MODE=adc
MASTER_SCRIBE_SCRIPT=/Users/josephocasio/Documents/New project/Master_Scribe_Final.py
OCR_PROMPT=Extract all text verbatim. Preserve all mathematical formulas and symbols exactly.
